---
name: to-prd
description: "RETIRED — use to-spec instead (renamed in v1.1)."
---

This skill was renamed to `to-spec` in Pocock v1.1.

Use `to-spec` instead. It does exactly the same thing with the correct name.
