---
name: to-issues
description: "RETIRED — use to-tickets instead (renamed in v1.1)."
---

This skill was renamed to `to-tickets` in Pocock v1.1.

Use `to-tickets` instead. It does exactly the same thing with the correct name.
